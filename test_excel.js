const ExcelJS = require('exceljs'); const wb = new ExcelJS.Workbook(); const ws = wb.addWorksheet('test'); ws.getColumn(1).numFmt = 'dd/mm/yyyy'; ws.addRow([new Date(2025, 8, 1)]); wb.xlsx.writeFile('test.xlsx').then(() => console.log('done'));
