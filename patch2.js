const fs = require('fs'); let code = fs.readFileSync('invoices.js', 'utf8'); code = code.replace('if (tbody) tbody.innerHTML = window.renderQuickAddRowHtml() + _tbodyHtml;', 'if (tbody) tbody.innerHTML = _tbodyHtml;\n      const thead = document.querySelector(\'#pi-table thead\');\n      if (thead) {\n        let qa = document.getElementById(\'qa-tr-row\');\n        if (qa) qa.remove();\n        thead.insertAdjacentHTML(\'afterbegin\', window.renderQuickAddRowHtml());\n      }'); fs.writeFileSync('invoices.js', code, 'utf8'); console.log('Done 2');
