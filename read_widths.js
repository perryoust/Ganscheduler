const fs = require('fs'); const ExcelJS = require('exceljs'); async function read() { const files = fs.readdirSync('./scratch'); const f = files.find(x => x.endsWith('.xlsx') && x !== 'user_invoices.xlsx'); const wb = new ExcelJS.Workbook(); await wb.xlsx.readFile('./scratch/' + f); const ws = wb.worksheets[0]; const widths = []; for(let i=1; i<=15; i++) { widths.push(ws.getColumn(i).width); } console.log('WIDTHS:', widths); console.log('FONT:', ws.getColumn(6).font); } read();
