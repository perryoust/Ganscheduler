const fs = require('fs'); let code = fs.readFileSync('invoices.js', 'utf8'); code = code.replace(/const quickAddHtml = window\.renderQuickAddRowHtml \? window\.renderQuickAddRowHtml\(\) : '';/, ''); code = code.replace(/tbody\.innerHTML = quickAddHtml \+/g, 'tbody.innerHTML ='); fs.writeFileSync('invoices.js', code, 'utf8'); console.log('Done 3');
